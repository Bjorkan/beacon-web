// One track list shared by the sticky header and every row, so columns stay aligned. Line 2 spans
// all of it via grid-column: 1 / -1.
export const GRID_TEMPLATE = "1.25rem minmax(7ch,auto) 6rem 5rem 3rem minmax(8rem,1fr) 3.5rem 5rem";
